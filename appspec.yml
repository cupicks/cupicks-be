version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/project
    overwrite: yes

file_exists_behavior: OVERWRITE

permissions:
  - object: /home/ubuntu
    pattern: '**'
    owner: ec2-user
    group: ec2-user

hooks:
  AfterInstall:
    - location: scripts/after-deploy.sh
      timeout: 300
      runas: ec2-user
      # runas는 사용자 이름