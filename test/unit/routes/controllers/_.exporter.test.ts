import * as exporter from "../../../../src/routes/controllers/_.exporter";

it("controllerExporter has 4 Controller", () => expect(Object.keys(exporter).length).toBe(4));
it("controllerExporter has 4 Controller", () => expect(Object.keys(exporter).length).toBe(4));
it("controllerExporter has 4 Controller", () => expect(Object.keys(exporter).length).toBe(4));
