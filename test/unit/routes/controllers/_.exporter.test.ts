import * as exporter from "../../../../src/routes/controllers/_.exporter";

it("controllerExporter has 5 Controller", () => expect(Object.keys(exporter).length).toBe(5));
it("controllerExporter has 5 Controller", () => expect(Object.keys(exporter).length).toBe(5));
it("controllerExporter has 5 Controller", () => expect(Object.keys(exporter).length).toBe(5));
